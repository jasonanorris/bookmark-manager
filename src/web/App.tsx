export function App(): JSX.Element {
  return (
    <main className="app-shell">
      <section className="intro" aria-labelledby="app-title">
        <p className="eyebrow">Self-hosted bookmarks</p>
        <h1 id="app-title">Bookmark Manager</h1>
        <p>
          The project scaffold is ready. The Worker API, D1 schema, web app, and
          browser extension will be built in the next phases.
        </p>
      </section>
    </main>
  );
}

